#include <bits/stdc++.h>

using namespace std;

int main()
{
    cin.tie(NULL)->sync_with_stdio(false);
    string str;
    while (getline(cin, str))
        cout << str << '\n';

    return 0;
}